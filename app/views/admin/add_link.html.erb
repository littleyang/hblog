<%= render :partial=>"header" %>
<div class="container">
  <div class="container-fluid">
    <div class="row-fluid post">
      <div class="span2">
        <%= render :partial => helper_check_permission(current_user) %>
      </div>
      <div class="span10">
        <div class="alert alert-info" style="height:18px"><p>当前时间:<%= Time.new().to_s(:long) %></p></div>
        <%= form_for :link,:url=>{:action=>:save_link,:controller=>"admin"},:html=>{:class=>"form-horizontal"} do |f| %>
          <legend>编辑连接</legend>
          <fieldset>
          <div class="control-group">
            <label class="control-label addform">名称</label>
            <div class="controls add">
              <%= f.text_field :title,:class=>"input input-xlarge" %>
            </div>
          </div>
          <div class="control-group">
            <label class="control-label addform">描述</label>
            <div class="controls add">
              <%= f.text_field :linkDesc,:class=>"input input-xxlarge" %>
            </div>
            </div>
            <div class="control-group">
            <label class="control-label addform">URL</label>
            <div class="controls add">
              <%= f.text_field :url,:class=>"input input-xxlarge" %>
            </div>
          </div>
          <% if @link %>
            <input type="hidden" name="id" value="<%= @link.id %>">
          <% end %>
          <div class="control-group">
            <div class="controls add">
              <%= f.submit "提交",:class=>"btn btn-success"%>
            </div>
          </div>
        </fieldset>
    <% end %>
      </div>
    </div>
  </div>
</div>
<%= render :partial=>"footer" %>
